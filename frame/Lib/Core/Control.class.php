<?php
class Control {
	public function site_is_valid($install_obj)
	{
		
		#$install_info = $install_obj->get_install_info();
		#
		#if (!$install_info['isuse'])
		#{
			#exit('网站已被关闭');
		#}
		#elseif ($install_info['endtime'] < time())
		#{
			#exit('网站已到期');
		#}
	}
}
